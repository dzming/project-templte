import { Context } from "@nuxt/types";

export default function ({ app, req, isServer, redirect }: Context) {
  // console.log(userAgent, 23213123123);
  // if (process.server) {
  //   return;
  // }

  // // const navigator = navigator;
  // console.log(navigator, 222);
  // if (navigator.language !== 'en-US') {
  //   console.log('iniin');
  //   redirect('/cn');
  // }
}